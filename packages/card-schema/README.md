# `@bos/card-schema`

Schema 1.0 TypeScript types and presenters for BenchOrStart cards. Shared later with Expo (`#139`) — types and copy only, no UI.

Face copy says **edge** (never `vs repl`). `sit` → **BENCH**. `approx` → **early model**.
